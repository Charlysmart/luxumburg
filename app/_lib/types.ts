type RoomProp = {
    id: string,
    name: string,
    slug: string
    size: string
    bedType: string
    maxGuests: string
    roomCount: string
    pricePerNight: string
    currency: string
    availability: string
    rating: string
    features: string[],
    images: string[]
}